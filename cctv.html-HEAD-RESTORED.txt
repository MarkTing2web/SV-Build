<!DOCTYPE html>
<html lang="en-GB">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>CCTV Camera Systems: Complete Guide for Singapore Properties | Securevision</title>
    <meta name="description" content="The definitive technical guide to CCTV camera systems in Singapore. Planning, resolution standards, AI analytics, and legal PDPA compliance for modern surveillance.">
    
    <!-- Google Fonts -->
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Montserrat:wght@400;500;600;700&family=Inter:wght@300;400;500;600&family=Outfit:wght@300;400;500;600;700&display=swap" rel="stylesheet">
    
    <!-- Shared Styles -->
    <link rel="stylesheet" href="sv-shared.css">
    <script src="site-config.js"></script>
    <link rel="stylesheet" href="sv-guides.css">
    
    <style>
        /* PAGE-SPECIFIC STYLE OVERRIDES */
        :root {
            --primary-cctv: #0056b3; 
            --primary-blue: #0046af;
            --primary-accent: #0056b3; 
        }

        .stat-card {
            background: rgba(255, 255, 255, 0.05);
            backdrop-filter: blur(10px);
            border: 1px solid rgba(255, 255, 255, 0.1);
            padding: 32px;
            border-radius: 12px;
            text-align: center;
        }

        /* -- Lush Camera Types Redesign -- */
        .lush-camera-grid, .lush-camera-grid-3 {
            display: grid;
            grid-template-columns: repeat(2, 1fr);
            gap: 32px;
            margin-top: 48px;
            align-items: stretch;
        }
        .lush-camera-grid-3 {
            grid-template-columns: repeat(3, 1fr);
        }
        .lush-camera-card {
            background: #fff;
            border: 1px solid var(--border-light);
            border-radius: 20px;
            overflow: hidden;
            transition: transform 0.3s cubic-bezier(0.4, 0, 0.2, 1), box-shadow 0.3s;
            display: flex;
            flex-direction: column;
            height: 100%;
        }
        .lush-camera-card:hover {
            transform: translateY(-8px);
            box-shadow: 0 20px 40px rgba(0, 0, 0, 0.08);
            border-color: var(--primary-cctv);
        }
        .lcc-image {
            height: 240px;
            background: #f1f5f9;
            overflow: hidden;
            flex-shrink: 0;
        }
        .lcc-image img {
            width: 100%;
            height: 100%;
            object-fit: cover;
            transition: transform 0.6s;
        }
        .lush-camera-card:hover .lcc-image img {
            transform: scale(1.08);
        }
        .lcc-content {
            padding: 32px;
            flex-grow: 1;
            display: flex;
            flex-direction: column;
        }
        .lcc-tag {
            display: inline-block;
            padding: 4px 12px;
            background: #f0f7ff;
            color: var(--primary-cctv);
            border-radius: 6px;
            font-size: 11px;
            font-weight: 700;
            text-transform: uppercase;
            letter-spacing: 1px;
            margin-bottom: 20px;
            align-self: flex-start;
        }
        .lcc-content h4 {
            font-size: 18px;
            font-weight: 700;
            margin-bottom: 12px;
            color: var(--text-dark);
            line-height: 1.4;
        }
        .lcc-content p {
            font-size: 14px;
            color: var(--text-gray);
            line-height: 1.6;
            margin-bottom: 20px;
        }
        .lcc-footer {
            margin-top: auto;
            padding-top: 20px;
            border-top: 1px solid #f1f5f9;
        }
        .lcc-spec {
            font-size: 12px;
            color: var(--text-light);
            display: flex;
            align-items: center;
            gap: 8px;
        }
        .lcc-spec strong { color: var(--text-dark); }

        @media (max-width: 1200px) {
            .lush-camera-grid-3 { grid-template-columns: repeat(2, 1fr); }
        }
        @media (max-width: 992px) {
            .lush-camera-grid { grid-template-columns: 1fr; }
        }
        @media (max-width: 768px) {
            .lush-camera-grid-3 { grid-template-columns: 1fr; }
            .lcc-image { height: 200px; }
            .lcc-content { padding: 24px; }
        }
        
    </style>
